<%= link_to activity_path(activity) do %>
<div class="card-trip">
  <%= cl_image_tag activity.photos.first.key%>

  <div class="card-trip-infos">
    <div>
      <%= link_to activity.name.capitalize, activity_path(activity) %>
      <p><%= activity.address.capitalize %></p>
    </div>
    <h2 class="card-trip-pricing"><%= activity.price%>€</h2>
    <%= cl_image_tag activity.user.photo.key, class: "card-trip-user avatar-bordered"%> 
  </div>
</div>
<%end%>